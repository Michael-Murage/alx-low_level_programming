#include "main.h"
/**
 * main - tests whether i is positive or negative
 * Return: 0 success
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	i = 0;
	positive_or_negative(i);

	return (0);
}
