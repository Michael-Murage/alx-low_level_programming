#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @a: first digit
 * @b: second digit
 * Returns: the result
 */
int add(int a, int b)
{
	int a;
	int b;
	int c = a + b;

	return (c);
}
